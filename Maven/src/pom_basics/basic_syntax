# Once a Maven project is created it will generate a POM.xml file.
# This file will contain the main configurations for the project.
# The basic structure outlined in the pom.xml file

project					Top-level element in all Maven pom.xml files
modelVersion			What object model the POM is using
groupId					unique identifier of the organization that created the project
artifactId				unique base name of the primary artifact being generated by this project
version					version of the artifact being generated by the project
name					Indicates the display name used
url						where the projects site can be found
properties				This element contains value placeholders accessible anywhere within the POM
dependencies			list of dependencies for the project
build					handles things like declaring your projects directory structure and managing plugins
